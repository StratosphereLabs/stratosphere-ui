export declare const MONTH_GRID_COLUMNS = 4;
export declare const DEFAULT_SINGLE_PLACEHOLDER = "Select date";
export declare const DEFAULT_DATE_TIME_PLACEHOLDER = "Select date and time";
export declare const DEFAULT_RANGE_PLACEHOLDER = "Select date range";
export declare const DEFAULT_MONTH_PLACEHOLDER = "Select month";
export declare const RANGE_SEPARATOR = " \u2013 ";
export declare const DEFAULT_TIME_LABEL = "Time";
/** The time a `datetime` selection starts at while no time has been picked. */
export declare const DEFAULT_TIME_VALUE = "00:00";
/**
 * Two day grids are 34.5rem wide together (two 15.75rem grids, the 2rem gap
 * daisyUI puts between them and the 1rem of padding), so `range` mode shows a
 * second month from 40rem up and falls back to a single month below it.
 */
export declare const TWO_MONTH_MEDIA_QUERY = "(min-width: 40rem)";
